# transport-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build transport-core` to build the library.

## Running unit tests

Run `nx test transport-core` to execute the unit tests via [Jest](https://jestjs.io).
