# transport-kafka

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build transport-kafka` to build the library.

## Running unit tests

Run `nx test transport-kafka` to execute the unit tests via [Jest](https://jestjs.io).
