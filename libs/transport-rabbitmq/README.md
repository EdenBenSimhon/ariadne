# transport-rabbitmq

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build transport-rabbitmq` to build the library.

## Running unit tests

Run `nx test transport-rabbitmq` to execute the unit tests via [Jest](https://jestjs.io).
