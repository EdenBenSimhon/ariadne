# sdk-nestjs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build sdk-nestjs` to build the library.

## Running unit tests

Run `nx test sdk-nestjs` to execute the unit tests via [Jest](https://jestjs.io).
